#!/usr/bin/bash

res=0

if [ "@REF_RESULTS_DIR@" != "" ]; then

    for f in solution/*.h5; do
        echo "Comparing $f against @REF_RESULTS_DIR@/c1/$f"
        h5diff -r --delta=1.e-6 $f @REF_RESULTS_DIR@/c1/$f
	if [ "$?" -neq 0 ]; then
            res=1
        fi
    done

fi

exit $res